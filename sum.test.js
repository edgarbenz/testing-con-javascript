const sum = require('./sum');


describe('2 tests', () => {
  test('TEST: 1 + 2 es igual a 3', () => {
    expect(sum(2, 2)).toBe(4);
  }); 
  test('TEST: 1 + 2 es igual a 3', () => {
    expect(sum(2, 2)).toBe(4);
  }); 
});

describe('2o. Tests', () => {
})